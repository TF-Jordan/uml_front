# {{ project_name }}

This project was generated by **UML2Code** from a UML class diagram.

## Requirements

- PHP >= 8.2
- Composer
- Node.js & NPM
- MySQL / MariaDB (or SQLite for development)

## Installation

1. Install PHP dependencies:
```bash
composer install
```

2. Install Node.js dependencies:
```bash
npm install
```

3. Copy the environment file and generate app key:
```bash
cp .env.example .env
php artisan key:generate
```

4. Configure your database in `.env` file

5. Run migrations:
```bash
php artisan migrate
```

6. (Optional) Seed the database:
```bash
php artisan db:seed
```

## Development

Start the development server:
```bash
php artisan serve
```

Or use the combined dev command:
```bash
composer dev
```

Build assets for production:
```bash
npm run build
```

## Generated Models

The following models were generated from the UML diagram:

{% for class_name in classes %}
- **{{ class_name }}** - `/app/Models/{{ class_name }}.php`
{% endfor %}

## API Endpoints

{% for class_name in classes %}
### {{ class_name }}
- `GET /{{ class_name | lower }}s` - List all {{ class_name | lower }}s
- `POST /{{ class_name | lower }}s` - Create a new {{ class_name | lower }}
- `GET /{{ class_name | lower }}s/{id}` - Get a specific {{ class_name | lower }}
- `PUT /{{ class_name | lower }}s/{id}` - Update a {{ class_name | lower }}
- `DELETE /{{ class_name | lower }}s/{id}` - Delete a {{ class_name | lower }}

{% endfor %}

## Project Structure

```
app/
├── Http/Controllers/     # Request handlers
├── Models/               # Eloquent models
├── Repositories/         # Data access layer
│   └── Interfaces/       # Repository contracts
└── Services/             # Business logic layer

database/
├── factories/            # Model factories for testing
├── migrations/           # Database migrations
└── seeders/              # Database seeders

routes/
└── web.php               # Web routes with CRUD endpoints
```

## Architecture

This project follows the **Repository Pattern** with a **Service Layer**:

```
Controller → Service → Repository → Model
```

- **Controllers**: Handle HTTP requests and responses
- **Services**: Contain business logic
- **Repositories**: Abstract data access
- **Models**: Eloquent ORM models

## License

This project is open-sourced software licensed under the [MIT license](https://opensource.org/licenses/MIT).
